// Call required plugins
var gulp = require("gulp"),
    del = require("del");


// Set project paths
var paths = {
  src: "src/",
  dest: "app/"
};


// Build pipes

gulp.task("default", function() {
  
});
